package com.devcraft.data.constant

class ApiEndpoints {
    companion object {
        const val ENDPOINT_NAME = "/test"
        const val GET_LEAGUES = "leagues"
        const val GET_STANDINGS = "leagues/{id}/standings?season=2021&sort=asc"
    }
}